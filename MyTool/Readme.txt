      这是我第一次利用PyQt5开发这样一个小项目，中间碰了很多壁，PyQt5网上的资料太少。鉴于自己对于Python的热爱，还是决定用PyQt，尽管python在音频，图像处理这个
领域不如MatLab（也确实好像如此。。。要调用很多不同的模块，库等等，例如opencv，还要自己下载ffmpeg，添加到环境变量。。然后再导入pydub进行音频处理，还有os模块，
numpy等等。。。），也可能是天资不够，在Qt的信号—槽函数上纠结了好几天，想了怎么在编译生成的子窗口.py文件直接定义相关函数，而不是再利用另外的控制文件（因为我
的主窗口文件只负责调用各个子窗口。。不然文件太多了。。），这也让我心疼那些跟我一样的，想用python做的陌生人。。。所以我准备将整个工程开源到GItHub上，有需要的
朋友可以联系我2507441744@qq.com



      下面重点说一下，软件的操作流程：因为我还不知道怎么在Qt中实现一个子程序的完全关闭，所以我在每一个子窗口加入了“最小化程序”这个按钮，相当于子程序在后台运行
但是主窗口不显示，所以当我们需要运行其他功能时，需要将当前的子窗口最小化（例如现在正在执行图像处理，想要进行视频取帧，需要点击当前窗口的最小化程序按钮，再点击
上方的视频取帧进入视频取帧的子窗口，其他窗口也是如此）

      运行call2mainwin.py文件！！！

      运行程序需要一定的前提准备，需要安装python进行相关处理的模块和库：openCV，ffmpeg， numpy， os，pyqt5等，可以采用pip的方式安装，各个模块的具体安装方法
在网上查询，都有相关介绍。如果程序有相关bug或者你有什么好的建议或者批评，欢迎随时联系我！
